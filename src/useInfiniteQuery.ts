import { InfinityReqParamsTypes } from "types";
import useQuery from "useQuery";

export default <T = any>(url:string, params: InfinityReqParamsTypes) => {

    const {} = params || {}

//   const {req, isError, isFetching, data, error, isLoading, isSuccess} = useQuery(url, params);

//   const updateQueryData = () => {}



  

//   return {
//     req,
//     isError,
//     isFetching,
//     data, 
//     error, 
//     isLoading, 
//     isSuccess
//   }
}